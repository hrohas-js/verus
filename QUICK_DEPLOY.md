# ⚡ Быстрое развертывание на бесплатном хостинге

## 🎯 Цель: Развернуть API за 5 минут

### Шаг 1: Создайте GitHub репозиторий
1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например: `verus-backend`)
4. Сделайте его публичным
5. **НЕ** добавляйте README, .gitignore или лицензию

### Шаг 2: Загрузите код в GitHub
```bash
# В папке с проектом выполните:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/verus-backend.git
git push -u origin main
```

### Шаг 3: Развертывание на Railway (Рекомендуется)

#### 3.1 Регистрация
1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Login" → "Login with GitHub"
3. Разрешите доступ к репозиториям

#### 3.2 Создание проекта
1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Найдите и выберите ваш репозиторий `verus-backend`
4. Нажмите "Deploy"

#### 3.3 Настройка переменных
1. В Railway Dashboard нажмите на ваш сервис
2. Перейдите в "Variables"
3. Добавьте переменные:
   ```
   APP_ENV = production
   APP_DEBUG = false
   DB_CONNECTION = sqlite
   DB_DATABASE = database/database.sqlite
   LOG_LEVEL = error
   ```

#### 3.4 Получение URL
1. Railway автоматически предоставит URL
2. Скопируйте URL (например: `https://verus-backend-production.up.railway.app`)

### Шаг 4: Проверка работы
```bash
# Проверьте здоровье API
curl https://YOUR_RAILWAY_URL/api/health

# Получите список оборудования
curl https://YOUR_RAILWAY_URL/api/equipment
```

---

## 🎉 Готово!

**Ваш API теперь доступен по адресу:** `https://YOUR_RAILWAY_URL/api`

### Примеры использования:
```bash
# Получить все оборудование
curl https://YOUR_RAILWAY_URL/api/equipment

# Добавить новое оборудование
curl -X POST https://YOUR_RAILWAY_URL/api/equipment \
  -H "Content-Type: application/json" \
  -d '{"title":"Монитор Samsung","quantity":5,"image":"monitor.jpg"}'

# Получить конкретное оборудование
curl https://YOUR_RAILWAY_URL/api/equipment/1
```

---

## 🔄 Автоматическое обновление

Теперь при каждом push в GitHub репозиторий Railway автоматически обновит ваш API!

```bash
# Внесите изменения в код
git add .
git commit -m "Update API"
git push origin main

# Railway автоматически развернет обновления
```

---

## 🆘 Если что-то пошло не так

### Проблема: Railway не может найти PHP
**Решение:** Убедитесь, что в корне проекта есть файл `composer.json`

### Проблема: Ошибка 500
**Решение:** 
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения установлены

### Проблема: API не отвечает
**Решение:**
1. Проверьте URL (должен заканчиваться на `/api/health`)
2. Подождите 2-3 минуты после развертывания

---

## 📱 Альтернативные платформы

### Render (альтернатива Railway)
1. Перейдите на [render.com](https://render.com)
2. "New +" → "Web Service"
3. Подключите GitHub репозиторий
4. Render автоматически использует `render.yaml`

### Heroku (классический выбор)
1. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)
2. `heroku create your-app-name`
3. `git push heroku main`

---

**Поздравляем!** 🎉 Ваш API теперь работает в интернете бесплатно!
