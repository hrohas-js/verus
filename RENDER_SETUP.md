# 🚀 Настройка Render.com для Verus Backend

## Проблема с портами

Если вы видите ошибку "Port scan timeout reached, no open HTTP ports detected", это означает, что приложение не открывает HTTP порт правильно.

## Решение

### 1. Обновите код в репозитории

Убедитесь, что у вас есть обновленные файлы:
- `render.yaml` (обновлен)
- `server.php` (новый файл)
- `routes/web.php` (обновлен)

### 2. Настройка переменных окружения в Render.com

В панели управления Render.com перейдите в раздел "Environment" и добавьте следующие переменные:

```
APP_NAME=VerusWarehouse
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-app-name.onrender.com
LOG_CHANNEL=stack
LOG_LEVEL=error
DB_CONNECTION=sqlite
DB_DATABASE=/opt/render/project/src/database/database.sqlite
```

### 3. Настройка базы данных

Render.com автоматически создаст SQLite базу данных по указанному пути.

### 4. Перезапуск развертывания

1. В панели Render.com нажмите "Manual Deploy" → "Deploy latest commit"
2. Или сделайте новый commit в main ветку

## Альтернативное решение

Если проблема продолжается, попробуйте изменить `startCommand` в `render.yaml`:

```yaml
startCommand: php artisan serve --host=0.0.0.0 --port=$PORT --no-reload
```

## Проверка

После успешного развертывания:
- Откройте ваш URL (например: https://your-app-name.onrender.com)
- Вы должны увидеть JSON ответ с информацией об API
- Health check доступен по адресу: https://your-app-name.onrender.com/api/health

## API Endpoints

После успешного развертывания будут доступны:

- `GET /` - Информация об API
- `GET /api/health` - Health check
- `GET /api/equipment` - Получение всей комплектации
- `POST /api/equipment` - Добавление итема комплектации
- `GET /api/equipment/{id}` - Получение конкретного итема
- `PUT /api/equipment/{id}` - Обновление итема
- `PATCH /api/equipment/{id}/quantity` - Изменение остатков
- `DELETE /api/equipment/{id}` - Удаление итема

## Устранение неполадок

### Если развертывание все еще не работает:

1. Проверьте логи в Render.com Dashboard
2. Убедитесь, что все файлы загружены в репозиторий
3. Проверьте, что переменные окружения настроены правильно
4. Попробуйте удалить и пересоздать сервис в Render.com

### Логи для отладки:

В Render.com Dashboard → Logs вы увидите:
- Процесс сборки (buildCommand)
- Процесс запуска (startCommand)
- Ошибки приложения

## Автоматическое развертывание

После успешной настройки:
- Каждый push в main ветку будет автоматически развертываться
- Render.com будет показывать статус развертывания
- Уведомления о статусе можно настроить в настройках проекта
