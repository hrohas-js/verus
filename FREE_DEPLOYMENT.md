# 🆓 Развертывание на бесплатных хостингах

## 🚀 Быстрый старт (3 варианта)

### Вариант 1: Railway (Рекомендуется) ⭐

**Railway** - отличный выбор для Laravel API с простым развертыванием.

#### Шаг 1: Подготовка
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub аккаунт
3. Создайте новый проект

#### Шаг 2: Развертывание
```bash
# 1. Загрузите проект в GitHub репозиторий
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/verus-backend.git
git push -u origin main

# 2. В Railway:
# - Нажмите "Deploy from GitHub repo"
# - Выберите ваш репозиторий
# - Railway автоматически определит PHP проект
```

#### Шаг 3: Настройка переменных окружения
В Railway Dashboard → Variables:
```
APP_ENV=production
APP_DEBUG=false
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite
LOG_LEVEL=error
```

#### Шаг 4: Получение URL
После развертывания Railway предоставит URL вида:
`https://your-project-name.up.railway.app`

**API будет доступен по адресу:** `https://your-project-name.up.railway.app/api`

---

### Вариант 2: Render

**Render** - хорошая альтернатива с простой настройкой.

#### Шаг 1: Подготовка
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Подключите GitHub аккаунт

#### Шаг 2: Развертывание
```bash
# 1. Убедитесь, что файл render.yaml находится в корне проекта
# 2. В Render Dashboard:
# - Нажмите "New +" → "Web Service"
# - Подключите GitHub репозиторий
# - Render автоматически использует render.yaml
```

#### Шаг 3: Настройка
Render автоматически настроит переменные из `render.yaml`:
- APP_ENV=production
- APP_DEBUG=false
- DB_CONNECTION=sqlite
- И другие...

**API будет доступен по адресу:** `https://your-service-name.onrender.com/api`

---

### Вариант 3: Heroku

**Heroku** - классический выбор, но с ограничениями на бесплатном тарифе.

#### Шаг 1: Подготовка
```bash
# Установите Heroku CLI
# Windows: https://devcenter.heroku.com/articles/heroku-cli
# Linux: curl https://cli-assets.heroku.com/install.sh | sh
```

#### Шаг 2: Развертывание
```bash
# 1. Логин в Heroku
heroku login

# 2. Создание приложения
heroku create your-app-name

# 3. Настройка переменных окружения
heroku config:set APP_ENV=production
heroku config:set APP_DEBUG=false
heroku config:set DB_CONNECTION=sqlite
heroku config:set DB_DATABASE=database/database.sqlite

# 4. Развертывание
git push heroku main

# 5. Выполнение миграций
heroku run php artisan migrate
```

**API будет доступен по адресу:** `https://your-app-name.herokuapp.com/api`

---

## 🔧 Автоматическое развертывание

### GitHub Actions для Railway

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Railway

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to Railway
      uses: railway-app/railway-deploy@v1
      with:
        railway-token: ${{ secrets.RAILWAY_TOKEN }}
        service: ${{ secrets.RAILWAY_SERVICE }}
```

### Настройка секретов в GitHub:
1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте:
   - `RAILWAY_TOKEN` - токен из Railway Dashboard
   - `RAILWAY_SERVICE` - имя сервиса

---

## 📊 Сравнение платформ

| Платформа | Бесплатный лимит | Авторазвертывание | Простота | Рекомендация |
|-----------|------------------|-------------------|----------|--------------|
| **Railway** | $5 кредитов/месяц | ✅ | ⭐⭐⭐⭐⭐ | 🥇 Лучший выбор |
| **Render** | 750 часов/месяц | ✅ | ⭐⭐⭐⭐ | 🥈 Хорошая альтернатива |
| **Heroku** | 550-1000 часов/месяц | ✅ | ⭐⭐⭐ | 🥉 Классический выбор |

---

## 🧪 Тестирование развертывания

После развертывания проверьте работу API:

```bash
# Проверка здоровья API
curl https://your-app-url.com/api/health

# Получение списка оборудования
curl https://your-app-url.com/api/equipment

# Добавление нового оборудования
curl -X POST https://your-app-url.com/api/equipment \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Monitor","quantity":3,"image":"monitor.jpg"}'
```

---

## 🛠️ Устранение проблем

### Railway
```bash
# Просмотр логов
railway logs

# Локальная разработка
railway run php artisan serve
```

### Render
- Логи доступны в Dashboard → Logs
- Автоматические перезапуски при сбоях

### Heroku
```bash
# Просмотр логов
heroku logs --tail

# Выполнение команд
heroku run php artisan migrate
```

---

## 💡 Советы по оптимизации

### Для бесплатных планов:
1. **Используйте SQLite** - не требует внешней БД
2. **Кешируйте конфигурацию** - `php artisan config:cache`
3. **Оптимизируйте автозагрузку** - `composer install --optimize-autoloader`
4. **Минимизируйте зависимости** - `--no-dev` флаг

### Мониторинг:
- Настройте health check endpoint
- Используйте внешние сервисы мониторинга (UptimeRobot)
- Логируйте важные события

---

## 🎯 Рекомендация

**Для быстрого старта рекомендую Railway:**
1. Простая настройка
2. Автоматическое развертывание из GitHub
3. Хорошая документация
4. Стабильная работа

**Начните с Railway, а при необходимости масштабирования переходите на Render или платные планы.**

---

**Готово!** 🎉 Ваш API будет доступен из интернета бесплатно!
